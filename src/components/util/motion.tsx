"use client";
import { motion } from "framer-motion";

// This allows us use framer-motion on the server side
export const MotionDiv = motion.div;
export const MotionLi = motion.li;
export const MotionP = motion.p;
